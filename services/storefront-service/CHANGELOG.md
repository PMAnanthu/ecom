# Changelog

All notable changes to storefront-service are documented here.
Format: `## [x.y.z] — YYYY-MM-DD` with `### Added / Changed / Fixed` subsections.

## [1.0.0] — 2026-08-07

### Added
- Initial release — public domain resolution endpoint (`/resolve?domain=<host>`)
- Returns storeId and store config for multi-tenant storefront rendering
