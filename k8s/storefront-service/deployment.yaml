apiVersion: apps/v1
kind: Deployment
metadata:
  name: storefront-service
  namespace: ecom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storefront-service
  template:
    metadata:
      labels:
        app: storefront-service
    spec:
      containers:
        - name: storefront-service
          image: ghcr.io/YOUR_ORG/ecom-storefront-service:latest
          ports:
            - containerPort: 3006
          env:
            - name: PORT
              value: "3006"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ecom-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecom-secrets
                  key: JWT_SECRET
            - name: JWT_REFRESH_SECRET
              valueFrom:
                secretKeyRef:
                  name: ecom-secrets
                  key: JWT_REFRESH_SECRET
          envFrom:
            - configMapRef:
                name: ecom-config
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3006
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: storefront-service
  namespace: ecom
spec:
  selector:
    app: storefront-service
  ports:
    - port: 3006
      targetPort: 3006
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: storefront-service
  namespace: ecom
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: storefront-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
